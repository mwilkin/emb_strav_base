import Ember from 'ember';

export default Ember.Component.extend({
  // userService: Ember.inject.service(),
  // userId: Ember.computed('userService.user', function() {
  //   return this.get('userService').user.id;
  // }),
  // racesByCurrentUser: Ember.computed.filter('races', function(race, index, array){
  //
  //   console.log(race.get('name'))
  //   console.log(race.creatorId)
  //   console.log(this.get('userService').user.id)
  //   console.log(race.get('creatorId') === this.get('userId'))
  //   return race.get('creatorId')===this.get('userId');
  // })
});
